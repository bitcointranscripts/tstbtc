# Make executable with chmod +x <<filename.sh>>
echo "What is your github username?"
read USERNAME

# cd .. && mkdir ${2} && cd ${2}

gh auth login

gh repo fork bitcointranscripts/bitcointranscripts --fork-name=bitcointranscripts --clone 

cd bitcointranscripts

# git init -b main
git checkout -b ${2}

cp ${3} .

git add "$1" && git commit -m 'initial transcription using yt2btc script'

gh pr create --title "Autogenerated yt2btc" --body "youtube to bitcoin transcript" --draft

# create a new repo, set the source, make it public, push and set remote branch
# gh repo create --source=. --public --push --remote=yttbtc

 

echo "Done"
# echo "Done. Go to https://github.com/$USERNAME/$2 to see."

# steps:
# - transcribe the file
# - cd one step back 
# - fork the transcript repo, clone it and branch out
# - copy the transcript file to the new dir
# - commit and push 
# - then open a PR 
